# AI 长视频自动切片项目

## 项目简介

本项目用于分析任何 `yt-dlp` 支持的公开视频链接或本地长视频，并生成基于创作者画像的粗剪计划。

程序会自动完成以下工作：

1. 下载视频或读取本地视频。
2. 使用 Whisper 生成带时间戳的字幕。
3. 用本地可解释的规则、创作者画像和人工反馈筛选精彩片段。
4. 根据人物面部位置自动裁剪画面。
5. 输出竖屏 MP4 视频及片段信息。

## 当前完成情况

- 支持 `yt-dlp` 可下载的公开视频平台。
- 支持本地视频文件和 `file://` 地址。
- 所有链接都只在本机处理，不存在云端/API 模式。
- 支持 Whisper 本地转录及字幕缓存。
- 支持视频下载缓存，避免重复下载。
- 支持自动人脸跟踪和竖屏裁剪。
- 已加入基础自动测试。
- 支持多平台一键画像粗剪：历史爆款画像、每小时筛选及带理由的粗剪计划。

## 项目目录

```text
ai劳工/
├── main.py                         # 程序入口
├── 项目说明.md                     # 中文项目说明
├── requirements.txt               # 本地处理依赖
├── requirements-local.txt         # 兼容原安装命令
├── tests/                          # 自动测试
├── skills/video/scripts/           # 可复用的视频工作流脚本
│   ├── creator_workflow.py          # 多平台一键画像、筛选、粗剪
│   ├── analyze_creator_profile.py   # 历史爆款字幕画像
│   ├── transcript_rough_cut.py      # 每小时候选筛选
│   ├── rough_cut.py                 # 原画幅、原音频 MP4 导出
│   └── local/                       # 下载与 Whisper 转写
└── output/                         # 生成的视频和字幕
```

## 在 VS Code 中运行

无需配置 `.env`、模型密钥或任何 API；下载、转写、筛选和粗剪都在本机完成。

本地转录默认使用 `faster-whisper`，建议在项目的 `venv` 中运行长任务。`whisper.cpp` 是可选后备后端；macOS 可安装命令行程序，并下载多语言模型到项目目录：

```bash
brew install whisper-cpp
mkdir -p models
curl -L -o models/ggml-base.bin \
  https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin
```

`base` 适合初筛；足球人名、队名和最终候选建议在本机使用 `small` 模型复核。若模型保存在其他位置，设置 `WHISPER_CPP_MODEL=/完整路径/模型.bin`。

### 1. 打开终端

在 VS Code 顶部菜单选择“终端 → 新建终端”。

### 2. 激活项目环境

```bash
source venv/bin/activate
```

### 3. 处理任意支持的视频平台

```bash
python main.py video-workflow "待剪视频链接或本地文件" --creator "博主名称"
```

无论使用任何 `yt-dlp` 支持的公开视频链接还是本地文件，都会使用同一条本地流程。

### 3.1 一键生成创作者画像并粗剪多平台长视频

为每条 `--history-url` 提供已确认的历史爆款。命令会转录这些视频并生成创作画像，再转录待剪直播，按每小时筛选，输出每段的时间、钩子、分数和选段理由。添加 `--export` 才会导出 MP4 粗剪：

```bash
python main.py video-workflow "待剪视频链接" \
  --creator "博主名称" \
  --history-url "历史爆款链接1" \
  --history-url "历史爆款链接2" \
  --output output/本次直播 \
  --export --reencode
```

输出目录包含：

- `creator-profile.json`：历史爆款字幕的高频主题、表达短语和人工备注；其高频主题/表达会叠加到选段信号中。
- `transcript.json`：待剪视频的带时间戳转录。
- `hourly-analysis.json`：每小时窗口的候选数与优先候选。
- `candidate-cut-plan.json`：最终选段及 `reason`（主题/人物、主播观点、反转或反应、台词梗等）。选段从话题起头保留到自然结束；不会为了凑固定时长在观点中间截断。过长且尚未结束的话题会留给人工审核。
- `clip-feedback.json`：人工反馈模板；填写后会用于下一次选段排序。
- `exports/`：仅指定 `--export` 时生成的原画幅、原音频 MP4 粗剪。

如果主播画像启用了 `special_segments.song`，转写命中“我唱”“唱一首”“清唱”等主播演唱提示词时，会单独按完整歌曲导出；歌曲不受 `--max-clips` 限制。`点歌`、`放歌` 等仅播放音乐的提示词可排除，避免误剪。该识别完全离线、依赖转写能识别到演唱提示词；未被转写出的纯音乐或歌词需要人工补充关键词或时间点。

常用参数：`--max-clips 8`、`--analysis-window-seconds 3600`（默认每小时）、`--candidates-per-window 5`、`--format 720`。`video-workflow` 对所有 `yt-dlp` 支持的公开视频站点统一采用同一条链路：画像、离线转写、完整话题筛选、人工反馈与粗剪。受登录、会员、DRM、地区限制或反爬保护的视频无法保证下载。

### 3.1.1 用 `clip-feedback.json` 迭代选段

首次运行会自动生成反馈模板。审核后填写每段的 `decision`（`keep`、`reject` 或 `adjust`）和 `reason`，并将会出现在未来字幕中的偏好词填入 `preferences`：

```json
{
  "preferences": {
    "boost_keywords": ["绝杀", "反讽", "主公"],
    "suppress_keywords": ["赛况播报"]
  }
}
```

下次运行时传入同一文件：

```bash
python main.py video-workflow "待剪视频链接" \
  --creator "博主名称" \
  --creator-profile skills/video/profiles/example-creator-profile.json \
  --clip-feedback output/上次任务/clip-feedback.json \
  --output output/本次任务
```

命中 `boost_keywords` 的候选会获得加分并在理由中标出“人工反馈偏好”；命中 `suppress_keywords` 的候选会被排除。还可以在 `instructions` 写入“完整话题后再切”等审核意见，作为该主播下次粗剪的固定标准。

已有画像时，使用 `--creator-profile` 跳过历史视频下载与分析：

```bash
python main.py video-workflow "待剪视频链接" \
  --creator "博主名称" \
  --creator-profile skills/video/profiles/example-creator-profile.json \
  --output output/本次直播
```

### 3.2 离线复跑

已有 SRT 时可跳过历史下载和目标转录，方便离线核验：

```bash
python main.py video-workflow "/完整路径/直播回放.mp4" \
  --creator "博主名称" \
  --history-transcript-srt "/完整路径/爆款1.srt" \
  --history-transcript-srt "/完整路径/爆款2.srt" \
  --target-transcript-srt "/完整路径/直播回放.srt" \
  --output output/离线复跑
```

## 输出结果

默认输出目录为：

```text
output/
```

其中通常包括：

- `source_*.mp4`：下载的视频缓存。
- `transcript.json`：Whisper 字幕和时间戳。
- `candidate-cut-plan.json`：按博主画像筛出的完整话题及理由。
- `exports/*.mp4`：仅使用 `--export` 后输出的粗剪。

## 运行自动测试

```bash
venv/bin/python -m unittest discover -s tests -v
```

## 常见问题

### 视频无法下载

先确认视频链接能在浏览器中正常访问。部分需要登录、会员或地区权限的视频可能还需要向下载器提供浏览器 Cookies。

### 没有识别出语音

视频可能没有清晰对白，也可能是背景音乐过强。可以尝试更大的 Whisper 模型：

```dotenv
LOCAL_WHISPER_MODEL=small
```

### 输出片段被覆盖

目前生成文件使用 `short_01.mp4`、`short_02.mp4` 等固定名称。处理下一条视频前，应先移动或重命名需要保留的成品。后续可以把“按视频建立独立输出目录”作为改进项。

## 后续开发建议

1. 按视频名称或 BV 号建立独立输出目录。
2. 自动为短视频添加字幕。
3. 增加中文图形界面，减少终端操作。
4. 增加片头、片尾、标题和水印模板。
5. 支持批量处理多个视频链接。
6. 为需要登录的公开视频平台增加 Cookies 配置说明。

# AI 长视频自动切片项目

## 项目简介

本项目用于把 YouTube、哔哩哔哩或本地长视频自动处理成适合抖音、TikTok、Instagram Reels 和 YouTube Shorts 的竖屏短视频。

程序会自动完成以下工作：

1. 下载视频或读取本地视频。
2. 使用 Whisper 生成带时间戳的字幕。
3. 使用大语言模型寻找适合传播的精彩片段。
4. 根据人物面部位置自动裁剪画面。
5. 输出竖屏 MP4 视频及片段信息。

## 当前完成情况

- 支持 YouTube 视频。
- 支持哔哩哔哩视频和 `b23.tv` 短链接。
- 支持本地视频文件和 `file://` 地址。
- B站链接会自动切换到本地处理模式。
- 支持 OpenAI 或 Gemini 分析精彩片段。
- 支持 Whisper 本地转录及字幕缓存。
- 支持视频下载缓存，避免重复下载。
- 支持自动人脸跟踪和竖屏裁剪。
- 已加入基础自动测试。

## 项目目录

```text
ai劳工/
├── main.py                         # 程序入口
├── README.md                       # 原始英文说明
├── 项目说明.md                     # 中文项目说明
├── requirements.txt               # 云端模式依赖
├── requirements-local.txt         # 本地模式依赖
├── shorts_generator/
│   ├── pipeline.py                 # 整体处理流程
│   ├── highlights.py               # 精彩片段分析
│   ├── muapi.py                    # MuAPI 请求
│   └── local/
│       ├── downloader.py           # 本地/B站/YouTube 下载
│       ├── transcriber.py          # Whisper 转录
│       ├── llm.py                  # OpenAI/Gemini 调用
│       └── clipper.py              # 本地裁剪和人脸跟踪
├── tests/                          # 自动测试
└── output/                         # 生成的视频和字幕
```

## 在 VS Code 中运行

### 1. 打开终端

在 VS Code 顶部菜单选择“终端 → 新建终端”。

### 2. 激活项目环境

```bash
source venv/bin/activate
```

### 3. 配置模型密钥

复制示例配置：

```bash
cp .env.example .env
```

编辑 `.env`，选择 OpenAI 或 Gemini：

```dotenv
LLM_PROVIDER=openai
OPENAI_API_KEY=填写你的密钥

# 或者使用 Gemini
# LLM_PROVIDER=gemini
# GEMINI_API_KEY=填写你的密钥
```

不要把 `.env` 或真实密钥提交到 Git。

### 4. 处理哔哩哔哩视频

```bash
python main.py "https://www.bilibili.com/video/BV号/"
```

程序检测到 B站链接后会自动使用本地模式。

### 5. 处理 YouTube 视频

本地模式：

```bash
python main.py "YouTube视频地址" --mode local
```

MuAPI 模式：

```bash
python main.py "YouTube视频地址" --mode api
```

### 6. 处理本地视频

```bash
python main.py "/完整路径/input.mp4" --mode local
```

### 7. 指定生成数量

```bash
python main.py "视频地址" --num-clips 5
```

程序默认生成 `16:9` 横屏视频。如需临时生成竖屏视频：

```bash
python main.py "视频地址" --aspect-ratio 9:16
```

### 8. 保存完整分析结果

```bash
python main.py "视频地址" --output-json result.json
```

## 输出结果

默认输出目录为：

```text
output/
```

其中通常包括：

- `source_*.mp4`：下载的视频缓存。
- `source_*.srt`：Whisper 字幕缓存。
- `short_01.mp4`：第一个短视频。
- `short_02.mp4`：第二个短视频。
- `short_03.mp4`：第三个短视频。

## 运行自动测试

```bash
venv/bin/python -m unittest discover -s tests -v
```

## 常见问题

### 提示缺少模型密钥

检查 `.env` 中是否填写了 `OPENAI_API_KEY` 或 `GEMINI_API_KEY`，并确认 `LLM_PROVIDER` 与密钥类型一致。

### 视频无法下载

先确认视频链接能在浏览器中正常访问。部分需要登录、会员或地区权限的视频可能还需要向下载器提供浏览器 Cookies。

### 没有识别出语音

视频可能没有清晰对白，也可能是背景音乐过强。可以尝试更大的 Whisper 模型：

```dotenv
LOCAL_WHISPER_MODEL=small
```

### 输出片段被覆盖

目前生成文件使用 `short_01.mp4`、`short_02.mp4` 等固定名称。处理下一条视频前，应先移动或重命名需要保留的成品。后续可以把“按视频建立独立输出目录”作为改进项。

## 后续开发建议

1. 按视频名称或 BV 号建立独立输出目录。
2. 自动为短视频添加字幕。
3. 增加中文图形界面，减少终端操作。
4. 增加片头、片尾、标题和水印模板。
5. 支持批量处理多个视频链接。
6. 为需要登录的 B站视频增加 Cookies 配置说明。

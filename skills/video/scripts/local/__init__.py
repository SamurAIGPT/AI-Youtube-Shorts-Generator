"""Local runtime components used by the video skill.

Used when the pipeline is invoked with mode="local". Requires the optional
deps in requirements-local.txt (yt-dlp, faster-whisper, opencv, moviepy).
"""
